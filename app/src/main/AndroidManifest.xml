<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tugas.myzodiak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyZodiak">
        <activity android:name=".hasil_zodiak"></activity>
        <activity android:name=".Activity_ramalan_shio" />
        <activity android:name=".Activity_carizodiak" />
        <activity android:name=".Activity_carishio" />
        <activity android:name=".ramalan" />
        <activity android:name=".cari_zodiak" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>